import NavbarHead from '../NavbarHeader/NavbarHead';

export const Header = () => {
    return (
            <div className="relative w-full">
                <NavbarHead />
            </div>
    );
}
